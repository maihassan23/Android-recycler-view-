<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mo.atef.amit.daythree.daysix_listviews">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MaterialCoBridgeTheme">
        <activity
            android:name=".activities.GridViewActivity"
            android:exported="true" >

        </activity>
        <activity
            android:name=".activities.ListViewActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".activities.ForLoopActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"> <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
    </application>

</manifest>